#include <stdio.h>

/**
 * main - this main is for printing a line.
 * You should always endeavor and make sure
 * to always follow the rules in character length
 * in a single line.
 *
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
