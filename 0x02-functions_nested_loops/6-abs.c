#include "main.h"

/**
 * _abs - function that prints the absolute value of an integer.
 *
 * @f: test number.
 *
 * Return: (0)
 */
int _abs(int)
{
	if (int < 0)
	{
		int = int * -1;
	}
	return (0);
}
