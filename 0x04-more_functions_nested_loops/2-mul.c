#include "main.h"

/**
 * mul - this function multiplies two integers.
 * @a: the first number
 * @b: the second number
 * 
 * Return: (0)
*/
int mul(int a, int b);
{
    _putchar(a + b);
    return (0);
}