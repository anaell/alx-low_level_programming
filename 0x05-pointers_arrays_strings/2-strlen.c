#include "main.h"

/**
 * _strlen - this function returns the length of a string.
 *
 * @s: the string to find length.
 *
 * Return: (0)
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
